LENS B walk (fresh end-to-end read of the ten largest remaining files, excluding the never-propose set): test_flow_worktree.py (131), test_seam_check.py (108), test_dispatch_stage.py (87), test_init.py (81), test_recall.py (79), test_diff_extract.py (68), test_stage_merge.py (62), test_evolve_reap.py (58), test_beads_adapter.py (54), test_evolve_select.py (53). Three proposals above; everything else below is a near-miss I opened and then rejected, with the mutation that saves it.

NEAR-MISSES REJECTED, WITH THE DISTINGUISHING MUTATION

1. test_recall.py:110 test_rank_top_n_limits_results vs :380 test_cli_returns_top_n. No distinguishing mutation found: recall.py slices once at line 232 (`scored[:top_n]`), cli_main passes top_n straight through (line 609/638/652) with no second slice, so every top_n mutation dies at both layers. Rejected NOT on coverage but on correlated-deletion risk: the only dominator is itself a thin CLI-wrapper test that a later CLI-lens wave could pull, and neither is in the survivor registry -- deleting the pure-function witness first would leave line 232 hostage to one integration test. Flagging it here rather than filing it.

2. test_evolve_select.py:48 test_budget_is_cap_minus_open_prs vs :592 test_budget_open_prs_dont_consume_concurrency. Also no sole-kill I could name -- I tried `budget = concurrency - inflight` (both die), `min(cap, concurrency) - open_prs` (both die), `cap - open_prs` alone (both survive, killed instead by :41 test_all_leaf_fans_out_to_concurrency), `len(cands) - open_prs` (both survive, killed by :41). Rejected because the pair is a two-point boundary probe of one `min()` and I have no mutation argument either way; deleting the base leaves the "discriminator" test (its own comment's framing) with nothing to discriminate against.

3. test_diff_extract.py:73 test_check_ownership_ok_when_only_planned_changed. Looks dominated by :121 (excludes_bootstrap_claude_dir) and :274 (committed_planned_change_ok), both of which assert ok is True with a single planned file. Rejected on brief: this is the content-ownership commit gate, a named correctness guard, and the whole cluster is off-limits.

4. test_diff_extract.py:48 test_since_returns_files_touched vs :362 test_since_multiple_files. Kept both: "files_touched returns only the first path" is killed only by :362; "insertions/deletions/binary absent or wrong on a single-file add" is killed only by :48 (the sole diff_since test asserting all four payload keys together).

5. test_recall.py:1017 test_digest_sections_grouped_in_canonical_order vs :1052 test_digest_all_six_valid_types_render. Kept both: :1052 writes its six entries already in canonical order, so an "emit in insertion order" mutation survives it and dies only at :1017 (which writes FACT before DECISION); conversely an "alphabetical section sort" mutation passes :1017 (DECISION<FACT<LEARNED is alphabetical) and dies only at :1052 (DEVIATION/INVESTIGATION/PATTERN).

6. test_recall.py:443 test_rrf_fuse_uses_reciprocal_rank vs :456 test_rrf_fuse_weights_bias_bm25_vs_cosine (which pins the same 1.0/(RRF_K+0) value). Kept: :443 is the only rrf_fuse call with an EMPTY second list, so a crash/short-circuit on an empty cosine list is killed only by it.

7. test_recall.py:153 test_ticket_exact_match_boosts_score vs :180 test_branch_and_ticket_boosts_stack. Kept: a mutation applying the ticket bonus only when branch_filter is also set passes the stack test and dies only at :153.

8. test_flow_worktree.py:756 test_lane_explicit_express_stamps_express vs :779 test_lane_explicit_wins_over_tier_label. Kept: `if not labels: return` (skip stamping when the bead carries no labels) is killed ONLY by :756 -- every other empty-label lane test (:763, :771, :810) expects the lane key to be ABSENT, so they all survive that mutation.

9. test_stage_merge.py:378 test_probe_ci_status_green_passthrough vs :497 test_probe_gate_argv_ci_status_present. Kept: the argv test never reads the verdict, so `result["ci_status"] = "pending"` hardcoded is killed only by :378 (with :386 covering the error arm).

10. test_stage_merge.py:874 test_cover_close_multiple_covers_all_processed vs :856 test_cover_close_hiccup_non_fatal_continues (both two covers, both count calls). Kept: "pass the first cover key to every call" is killed only by :874, which asserts keys_touched == {flow-y, flow-z}; the hiccup test counts calls by type, not by key.

11. test_init.py:1393 test_reconfigure_freezes_value_differing_from_current_default vs :1408 test_reconfigure_preserved_warning_names_value_and_default (byte-identical setup). Kept both: "compute the warning from the prior value, then overwrite handlers[stage] with the registry default" dies only at :1393; "warning text drops the registry default" dies only at :1408.

12. test_init.py:1210 test_init_gitignore_appends_preserving_existing vs :1189/:1199. Kept: it is the only fixture with an EXISTING .gitignore carrying no flow block at all, so "when the file exists, append only the pool line and skip the .flow/* block" is killed only by it (:1189 and :1199 already contain .flow/*, :1180 takes the create-from-absent branch).

13. test_beads_adapter.py:215 test_get_marshals_text_keyed_comment vs :226 test_get_falls_back_to_body_keyed_comment. Kept both: they pin opposite arms of the text/body comment key, and :173 (full ticket shape) asserts only body["fmt"], never the body text.

14. test_evolve_reap.py covers cluster (:467, :481, :487, :494, :507, :514) and the --limit 0 pair (:856, :893). All kept: empty-trailers vs absent-commits-key vs lead-own-key vs held_main_red-entry vs cross-commit dedup each kill a different arm of the trailer parse, and the two --limit tests guard two different call sites (_labels_index vs _file_main_red_p0).

15. test_dispatch_stage.py finish/output-path cluster (:624, :641, :658, :678, :698) and the recovery-marker trio (:1669, :1685, :1700). All kept: each output-path test pins a different rejection/acceptance arm (missing / existing / advance-does-not-advance / relative-resolution / directory), and each marker test covers a different verb's return path.

16. test_seam_check.py: no near-miss reached the point of a written candidate. Its shape is one hand-built prose fixture per diagnostic, with the live-corpus tests (:621, :944, :964, :1093, :1164) pinned to the real docs -- those are live pins and out of scope, and the fixture tests each assert a distinct error string.

Files walked with zero candidates: test_seam_check.py, test_dispatch_stage.py, test_flow_worktree.py, test_stage_merge.py, test_evolve_reap.py, test_diff_extract.py, test_init.py, test_evolve_select.py. Candidates came only from test_beads_adapter.py (1) and test_recall.py (2).

=====

LENS A = per-test BRANCH-ARC coverage subsumption, a structural view none of the six prior lenses could take. Method: one full-suite run under coverage 7.14 with `--cov-branch --cov-context=test` (3,166 contexts, 434,964 engine arcs, DB at /Users/victordsm/.claude/jobs/34298248/tmp/lensB.coverage), then two computations — (a) strict arc-subset pairs and (b) arc-EQUALITY classes — each intersected with AST-normalized assertion-text subsumption, with the 257 registry witnesses and this session's refutation list removed. That yielded 78 strict-subset candidates and 183 equal-arc pairs. I hand-adjudicated every one with an explicit sole-kill search; all but the three filed above had a mutation killed by them alone. Everything below is now SWEPT and should not be re-walked.

Refuted (each is genuine sole coverage — the reason is recorded so the next round can skip it):
- Short-circuit collisions on one source line (arcs coincide, semantics differ): metric.classify_attribution reflect-missing vs reflect-not-completed (line 214, `not isinstance(reflect, dict)` vs status!=completed — dropping the isinstance raises AttributeError only in the first); memory_append empty-labels-list vs absent-labels (`if labels:` vs `if labels is not None:`); triage adjudicate_hot/advisor key-absent vs explicitly-false (default flip kills only key-absent); observe_ship_event validate missing-evidence vs non-object-evidence; validate_workspace label_facets non-list vs non-str-element (dropping isinstance(list) lets the scalar "form" pass as an iterable of str).
- Corpus/data rows where the varied value IS the claim: forge_github pr_info empty vs null; lease boot_id OSError vs nonzero and session_pid OSError vs CalledProcessError; scrub_ci_skip near-miss trio; init _resolve_checkpoint_mode jira/personal vs beads/work; evolve_common run_dir_for pool vs claude-pool vs legacy-feature layouts (each pins one glob in the pattern list); flow_worktree epic-case-insensitive vs epic-plain; pending_mutations edit-op vs invalid-op.
- Ordering/precedence witnesses in test_hitl_gating: floor_hitl_wins_over_hot, floor_hitl_fires_even_when_adjudicate_hot_on, floor_refuses_hitl_undecided each kill a different reorder of the hitl/hot gate. All three stay.
- forge_bitbucket bot_review_present INPROGRESS vs check-absent: INPROGRESS alone kills `return state is not None`.
- senses_deadman cover_self_reference_alone_does_not_cover: alone kills `observed_keys | {child_key}`.
- scrub_ci_skip skip_checks_idempotent vs bracketed idempotent: alone kills a colon-optional trailer regex (the neutralized-trailer test still passes under it).
- maintainer_preflight staleness_fresh_runs_are_silent: sole witness of the FRESH side of the weekly 8d threshold (the arc-superset test seeds no weekly record at all).
- worker_pool capture_git_snapshot_is_stable_with_preexisting_dirt: sole witness that a NON-EMPTY tracked diff hash and a NON-EMPTY untracked digest are deterministic across back-to-back captures — detects_each_worktree_class never re-captures a non-empty untracked digest, so an mtime added to the digest would slip past it.
- triage decided_hot_via_guard_set: sole witness that `--files` actually feeds hotness (dropping the is_hot_change term leaves every other triage test green).
- recall rrf_fuse_weights_default_is_equal: same signature-default class as the already-refuted pr_body default-cap test; not re-proposed.
- review_brief absent_or_non_boolean_unattended_fails_closed rows: the "yes"/1 rows kill truthy-vs-`is True` and `== True`; parametrize-row surgery is out of scope anyway.
- Self-referential-oracle scan (separate AST pass: 26 tests whose assert has module-under-test symbols on BOTH sides) turned up nothing deletable — e.g. the machinery_edit _GIT_ERROR trio is three genuinely distinct git failure modes (nonzero abbrev-ref, OSError, nonzero is-inside-work-tree).
- Excluded by standing rule and not adjudicated: test_lease.py, test_locking.py, test_harness_corpus.py, live pins (test_module_map live_generated_blocks, seam_check live-docs), and the correctness-guard arc-equal clusters in test_diff_extract.py (content-ownership gate) and test_dispatch_stage.py (canonical-snapshot TOCTOU).

Dead-parametrize-axis sub-sweep: clean. Every multi-row parametrize either maps rows to distinct engine branches or is already collapsed with a mutation-verified comment (test_compose_commit.py's single-row `["feat"]` axis and test_bundle_discover.py's one-representative field axis are the visible residue of earlier collapses; the remaining rows in test_lint_comments word/suffix lists, test_public_commands removed-form tokens, test_model_resolve disabled-value lists, and test_observe_ship_event FIELDS are all corpus-membership pins).

No repo state was modified: all mutation experiments ran in a scratch copy under /Users/victordsm/.claude/jobs/34298248/tmp/lensA_mut, and every full-suite confirmation used `--deselect`. `git status --porcelain` is empty.

===== TURN 5 =====

LENS C = tests that execute ZERO engine statements, plus the two structural relations arc-subsumption cannot express (union-domination and cross-module mirrored delegation). Method: collected all 3,161 node ids and diffed them against the 3,064 contexts in lensB_cov.pkl, which isolates 102 nodes across 19 files that never enter engine code -- a population lens A structurally could not relate, since it works on non-empty arc sets. Then an AST pass for constants-only tests (assert reads only a module-level CONSTANT, no engine call), a complete engine-wide scan for one-line `return <imported helper>(...)` delegations reachable from two modules, and a complete scan for test-function NAMES duplicated across files (43 names). All mutation work in a scratch copy at /Users/victordsm/.claude/jobs/34298248/tmp/r5mut; repo read-only throughout (git status --porcelain empty).

NEAR-MISSES OPENED AND REFUSED, WITH THE DISTINGUISHING MUTATION

1. The queue/evolve mirrored BUDGET cluster (test_all_leaf_fans_out_to_concurrency, test_budget_is_cap_minus_open_prs, test_budget_open_prs_dont_consume_concurrency, test_budget_subtracts_inflight_count, test_backpressure_empties_launch -- same names at test_queue_select.py:41/48/396/387/54 and test_evolve_select.py:41/48/592/583/79). Same shape as the filed find: both call sites are byte-identical (`backpressure_budget(cap, open_pr_count, concurrency, inflight_count)`, queue_select.py:103 / evolve_select.py:101) and the arithmetic is one shared `min()`. Battery result: B2 ignore-inflight kills 5, B3 ignore-open-prs kills 4, B4 min->max kills 14, always symmetrically on both sides; per-call-site arg swaps (B5-B8) each kill 4-5 tests on that side alone. So no node in the cluster is a sole killer -- structurally the cluster is doubled. NOT FILED: the refutation record already adjudicated and refused test_evolve_select.py:48 vs :592 by name, and cited :41 as the killer that saves other mutants; any cut here re-proposes an adjudicated node. Recorded so a later round does not re-walk it. (Side observation, a gap not a redundancy: B1, dropping the `max(0, ...)` floor in backpressure_budget, kills NOTHING in the suite.)

2. metric CLI mirror family: test_cli_namespace_required x4 (test_metric_arm_compare:396, test_metric_corpus_health:235, test_metric_friction:214, test_metric_revert:356) and test_cli_no_flow_dir x5, each a one-line `metric.cli_main([<subcommand>, ...])` with the same rc/stderr claim. Refused: metric.py copy-pastes the guard per handler -- 7 occurrences of `if not args.namespace` and 9 `_check_flow_dir(workspace_root)` call sites -- so each mirrored test is the sole witness of ITS handler's copy; deleting one leaves that `_run_*` unguarded with nobody red. (Also: test_cli_no_flow_dir and test_cli_happy_path are registry names.)

3. test_hitl_gating.py:66 test_queue_select_excluded_labels_contains_hitl (`"hitl" in queue_select._EXCLUDED_LABELS`) vs :70 test_queue_select_partition_drops_hitl_keeps_twin, which pins the same label BY LITERAL through partition. Every natural mutation of the set or of partition's filter kills :70 too. Refused on the second consumer: queue_drain.py:142 imports the same constant for the stranded in_progress scan, and no test anywhere else passes a hitl label (grep: hitl appears only in test_hitl_gating.py plus one triage result dict), so :66 is the only test that notices the label leaving the shared set on that path. Stays.

4. Constants-only tests where the behavioural tests use the SYMBOL, not the value -- all refused, and this is the rule that separates them from the filed find: recall.py's test_ticket_bonus_stronger_than_branch_bonus (test_recall.py:162) is the sole killer of a TICKET/BRANCH value swap because every scoring test asserts `base + recall.TICKET_EXACT_BONUS` symbolically; test_fleet.py:29 test_stale_after_s_covers_longest_default_stage_lease is a bare `== 7200` literal and every staleness test reads the symbol (test_fleet, test_launch_window_equivalence), so it is the only witness of the value; test_lifecycle.py test_action_vocabulary_is_closed is the only test that fails when an action is ADDED. All stay.

5. test_flowctl.py:14 test_hot_path_command_resolution (15 facade mappings) and :35 test_default_commands_use_kebab_case (4 mappings + 6 deliberate absences). Disjoint key sets, so no pairwise domination. I also weighed the seam checker's live-docs run as an external dominator for the mapping half (a wrong or missing COMMANDS entry makes prose facade invocations resolve to the wrong script's argparse) -- refused: the 6 absence assertions (`init`, `agent-route`, `cognitive-worker`... deliberately not exposed through the facade) are killed by nothing else, and leaning on prose invocation coverage would make the mapping witness hostage to whatever the docs happen to invoke.

6. test_handler_grammar.py test_validate_handler_re_unchanged (38 rows, zero engine arcs -- it only matches the compiled HANDLER_RE). Refused: it is the sole witness of the charset-STRICT arm. Its two file-mates exercise the lax runtime paths (init._legal_handler_string, resolve_handler.resolve), and test_validate_workspace.py never feeds an illegal handler string (the whitespace battery values live only in this file), so widening HANDLER_RE goes unnoticed everywhere else. Whole-function deletion, not row surgery, and still refused.

7. test_tracker.py's no-arc six: test_tracker_error_is_exception (a `BaseException` base-class mutant survives every `pytest.raises(TrackerError)` site while silently escaping engine `except Exception` handlers) and the Protocol pair test_fake_adapter_is_structurally_a_tracker / test_object_missing_methods_is_not_a_tracker (opposite directions: adding a method to the Protocol kills only the first, removing one kills only the second). All stay; two of the six are registry names anyway.

8. test_git_hermeticity.py's three conftest guards. Refused: each probes a different config key (core.fsmonitor/core.untrackedCache, diff.external, user.email + default branch), and the leak class they guard is real in this repo (the difftastic diff.external note in project memory). One shared mechanism does not make the per-key witnesses fungible -- a per-key fix would leave the other keys unguarded.

9. The rest of the no-arc population is out of scope by the standing rule, and I did not adjudicate it: test_seam_check live-docs (18 nodes), test_registry.py::test_reference_doc_resolves (11 rows against the real stage-registry.toml), test_plugin_packaging (6, real manifests/workflow/.gitignore), test_stage_code_review_phrasing (4 prose pins on the real doc), test_harness_corpus (3), plus the process/concurrency singletons that show no arcs because the work happens in a child process (test_flow_worktree concurrent_bootstraps, test_observe_ship_event concurrent_observers_race_o_excl, test_flow_launcher python311 facade, test_revise_config's two subprocess CLI tests, test_ops_templates bash wrapper).

SCANS NOW EXHAUSTED (do not re-walk): the zero-engine-arc population (all 102 nodes enumerated and triaged above); the constants-only AST scan (20 hits, all listed or filed); the engine-wide mirrored-delegation scan, which returned exactly two shared helpers -- _evolve_common.read_cap_concurrency (filed) and tracker.make_tracker, refused because both wrappers (sync._build_tracker, worktree_janitor._load_tracker) are monkeypatched in their tests, so no arm duplication exists; and the cross-file duplicate-name scan (43 names: 23 in the queue/evolve mirror, 5 in the forge_github/forge_bitbucket pairs which are two separate adapter implementations and already refused in the record, the metric CLI family in item 2, and the rest thin per-script CLI shells whose names collide by convention).

===== TURN 5 =====

SWEEP 2 ANGLE (deliberately disjoint from sweep 1 and from lenses A/B): mutation-independent deadness plus the one structural gap lens A's method left open. Lens A required (engine-arc subset) AND (AST-normalized assertion-text subsumption). Two shapes escape that intersection, and I went after both.

GAP 1 — same claim, arcs differ in BOTH directions (duplicated engine code). Scanned all 2,696 test functions for structurally identical bodies after erasing names/strings/numbers. 10 collision groups, all cross-file, all refuted:
- test_metric_{arm_compare,corpus_health,friction,revert}.py::test_cli_namespace_required and ::test_cli_no_flow_dir (4 copies each, byte-identical but for the subcommand token). REFUTED: metric.py duplicates the guard per subcommand — `if not args.namespace` at 1488/1514/1540/1563… and a separate `_check_flow_dir` call site inside each `_run_*`. Deleting the arm-compare copy leaves `_run_arm_compare`'s own guard unwitnessed. All 8 stay.
- 7 pairs shared by test_evolve_select.py / test_queue_select.py (select_pre_pr_live_run_is_inflight, select_fleet_only_key_inflight_but_absent_from_live_runs, select_drops_inflight_branch, select_not_maintainer_raises, select_plain_bead_no_downshift, select_worker_model_plain_bead, select_tool_error) plus test_evolve_drain/test_queue_drain::test_cli_not_maintainer_dormant_exit_4. REFUTED: queue_select.py is a genuine fork, not a re-export — its own `_day_job` filter, its own `partition`, and a `select` that subtracts `active_evolve_keys` from BOTH backpressure terms (lines 159-171) with no counterpart in evolve_select. Each copy is sole coverage of its module's call site. (Several are registry-protected by name anyway, and the registry being name-keyed means both files' copies are protected.)

GAP 2 — arc-subset pairs whose assertions coincide only after TEST-CHOSEN LITERALS are erased (lens A's normalizer evidently kept literals, so these never entered its 78+183). Recomputed subset/equal pairs from lensB.coverage restricted to engine arcs (3,064 contexts, 9,263 ordered pairs), then kept only pairs where every raw-unmatched candidate assertion differs from its dominator counterpart solely in a literal the test itself passes in as an argument — an input echoed to output, which by construction cannot select a branch. 76 candidates. After removing everything the record already adjudicated (evolve_select budget pair, flow_worktree lane_explicit_*, init reconfigure_freezes_*, stage_merge probe_*, recall rrf_fuse_uses_reciprocal_rank, triage decided_hot_via_guard_set, senses_deadman cover_self_reference_alone, forge_bitbucket bot_review_present, machinery_edit branch trio area, diff_extract ownership cluster, seam_check, the never-propose files), the residue splits into families I refuted by hand:
- "the differing literal IS the claim" — reflect_inputs reflect_config_{defaults_when_no_block,block_overrides} vs partial_block (the dict values are the default/override arms); ticket_frontmatter update_dotted_key_stays_flat vs update_key_with_space (dot-no-nesting vs space-no-quoting, distinct serializer claims); resolve_handler skill_no_args_yields_empty_string vs skill_present_and_valid (kills `else None` — `''` is the claim) and skill_multi_segment_args_preserved (kills a split-at-first-colon truncation); version cli_commit_type_defaults_empty vs cli_stamp_plumbs_commit_type (flag-absent default vs explicit plumb); revise_config invalid_value_falls_back_to_default vs below_minor_floor_rejected (unparseable vs below-floor are two rejection reasons); queue_select/evolve_select epic + evolve/proposal/hot/hitl label tests (each pins one member of `_EXCLUDED_LABELS`).
- CLI-wrapper correlated-deletion (record's own note 1) — ticket_frontmatter update_creates_file_if_missing vs cli_update_persists: deleting the pure-function witness makes create-if-missing hostage to a CLI test.
- machinery_edit feature_branch_still_applies / non_repo_branch_none_still_applies: the success counterparts of the already-refuted _GIT_ERROR trio; one kills "refuse unless on main", the other "refuse when branch is None".

The one candidate that survived reading, and how it died: test_compose_commit.py::test_includes_ticket (L29). Its 12 engine arcs are a verified strict subset of test_covers_emit_closes_lines' 13 (the single extra arc is the genexpr loop-back at compose_commit.py:52), its lone assertion `'ticket: FT-77' in out` is the dominator's assertion modulo an inert ticket key, and its input is the dominator's input minus `covers`. I ran the mutations in a scratch copy with bytecode caches cleared between runs: dropping the trailer from line 49 reds BOTH (2 failed), but making the trailer conditional on `covers` being non-empty reds test_includes_ticket ALONE (1 failed), and the mirror condition reds the dominator alone. So the two are a symmetric two-arm witness that the `ticket:` trailer is unconditional with respect to covers — the same shape as the ordering/precedence witnesses kept earlier. STAYS. (Its file-mates test_body_template_present, test_no_files_section_when_empty, test_no_closes_when_no_covers, test_scope_optional are arc-EQUAL on an identical call but assert four textually and semantically disjoint claims — `# body` present, no `files:` header, no `Closes ` footer, no scope parens — each the sole killer of its own template mutation. All stay; note `out.count("Closes ") == 1` does not imply `"Closes " not in out`.)

OTHER PASSES, ALL CLEAN (do not re-walk):
- Name-shadowed tests (a duplicate def in a module makes the earlier one uncollectable dead code): ZERO across all 114 test files.
- Never-executing tests: zero unconditional skip/xfail. The only skip is test_flow_launcher.py::test_python311_launcher_installs_facade_that_can_run_lifecycle, a legitimate environment-conditional skipif on `shutil.which('python3.11')`.
- Tests with no effective oracle: 16 raw hits, 12 inside the never-propose files. The other four all carry a real "does not raise" oracle and are sole witnesses: fleet::test_deregister_missing_key_is_noop (kills the missing-dir/entry early return — every other deregister test registers first); hitl_gating::test_floor_passes_hitl_decided (kills `if hitl: raise`, ignoring `decided`) and ::test_floor_passes_unlabeled (kills dropping the `hitl` term — it is the only bead with hitl False AND decided False, so `if not decided: raise` reds it alone); flow_worktree::test_bootstrap_claim_released_after_success (the release-on-success path, vs …_after_refusal's release-before-raise).
- Museum pieces (engine symbols reachable only from tests, so the test guards nothing the pipeline runs): with reference counting done correctly over all engine code plus prose/toml/json, exactly one — `public_commands.Registry.root_tokens` (public_commands.py:112), whose only consumer repo-wide is test_public_commands.py:85 inside test_worktree_cleanup_is_documented_as_workspace_local_two_pass. NOT proposed: that test is a live pin on the authored public-commands.toml and references/command-maintain.md, and the "public grammar is generated / removed public forms must fail" invariant is exactly what it guards. Worth noting for a human, though: `root_tokens` is production code with no production caller, and its file-mate test_real_registry_is_complete_and_has_no_legacy_root_verbs (L26) asserts only `static_namespaces` — the legacy-root-verb claim its name promises actually lives in the next test. That reads like a botched split, not redundancy; both tests assert distinct things and both stay.
- Unrelated observation, surfaced because the same pass found it and it is the inverse of a deletion candidate: tracker_beads.py:594 `_verify_field` (a ~45-line post-write postcondition re-reader with its own labels/assignee/priority normalization and four distinct TrackerError strings) has ZERO callers and ZERO tests anywhere in the repo — the only other mention is a prose comment at test_beads_adapter.py:251 claiming it is exercised. The live postcondition path is the inline check at tracker_beads.py:438, which is what the registry-protected test_transition_postcondition_mismatch_returns_failure covers. Either a wiring regression or intentional dead code; a human should decide, and it is not a suite-reduction item.

No repo state was modified: every mutation and removal ran in /Users/victordsm/.claude/jobs/34298248/tmp/sweep2_mut (a copy), and `git status --porcelain` in the worktree is empty at HEAD f75ea9c. Note for anyone re-running mutations there: `cp -R` carries `__pycache__` along, and a same-byte-length mutation can be masked by stale bytecode — clear `__pycache__` and `.pytest_cache` between runs (my first M1-M4 pass was contaminated this way; the reported results are from the cleaned re-run). Scratch-copy baseline is 9 pre-existing failures (plugin-manifest and live-plugin-version path pins that need the real checkout), not 0.

===== TURN 5 APPLIED =====
Cut: evolve-side config_defaults invalid_values/no_workspace mirror pair (queue twins now protected registry members), test_version PLUGIN alias.

=== TURN 6 (2026-07-25) — 1 CONFIRMED_DELETE, 0 REFUTED ===
Applied cut: tests/test_evolve_reap.py::test_guard_file_dirty_no_label_is_blocked
(input-blind; commit 94a3caf carries the full battery: M1/M7/M-A blindness incl.
the reintroduced flow-1fy defect, M3/M4 kill-set shared with
test_hot_dirty_is_blocked + test_hot_auto_merge_clean_promotes_dirty_blocks,
entry-shape asymmetry probe caught by 2 tests post-deletion).

Sweep 2 (epistemics-inversion angle over the same 485 no-single-dominator
contexts) filed ZERO proposals.

Finder near-misses adjudicated and NOT proposed (re-proposing any is a hard miss):
- test_init reconfigure pair — flagged "strongest remaining near-miss" but NOT
  adjudicated empirically by the finder; any future proposal must start with a
  fresh mutation battery, no inherited credit.
- six further guard-file / hot-arm neighbors rejected during the turn-6 sweep
  because each had a distinguishing mutation (sole kill) in the finder's battery;
  the six covering witnesses were added to the survivor registry (266 entries):
  test_hot_dirty_is_blocked, test_hot_auto_merge_clean_promotes_dirty_blocks,
  test_guard_file_green_clean_skipped_hot_when_off,
  test_guard_file_promotes_with_is_hot_true,
  test_guard_file_and_label_hot_serialize,
  test_triage.py::test_decided_hot_via_guard_set.

=== TURN 7 (2026-07-25) — 2 CONFIRMED_DELETE, 0 REFUTED among proposals; reconfigure pair adjudicated KEEP ===
Applied cuts (commit d61dc5a, full batteries in the message):
- tests/test_init.py::test_stabilize_skill_dir_non_cache_unchanged
- tests/test_init.py::test_stabilize_skill_dir_cache_but_marketplace_missing_unchanged
New lens on record: single-module unit tests living outside their own
test_<module>.py file (91 expats scanned; only these two provable duplicates).
New registry carriers (270 entries): test_flow_launcher.py::test_stabilize_skill_dir_fails_safe,
::test_claude_harness_does_not_fall_through_to_codex_source_collision,
::test_generic_harness_does_not_guess_native_marketplace_source,
test_init.py::test_stabilize_skill_dir_rewrites_cache_to_marketplace.

test_init reconfigure pair — ADJUDICATED KEEP, empirically (was "not adjudicated"):
- test_refuses_when_already_initialized: SOLE KILL M3 (refusal message stripped of
  the marker path) reds it alone; it is the only suite-wide observer of the refusal
  message (test_cli_preflight_exit_code asserts rc==4 only).
- test_reconfigure_clears_prior_markers: SOLE KILLS M16/M17/M18/M20 — only test that
  reconfigures a marker-only workspace (no prior workspace.toml/runtime), sole witness
  of the missing-prior-config fail-safe.
Adjacent pairs also KEEP with recorded discriminators: refuses_when_initializing_
without_resume (M23/M14 sole), successful_reconfigure_swaps_workspace (M22 sole),
successful_reconfigure_starts_with_a_fresh_run_id (M8 sole).
Re-proposing ANY of the above is a hard miss.

Coverage GAPS logged (not deletions; candidate material for a follow-up ticket):
reconfigure-time .init-progress unlink unpinned (M6, 0 kills); stale progress
ledger inert on reconfigure (M9, 0 kills); cache-path length guard under-pinned
(cache_index+6->+5 and dropped disjunct both survive); claude-branch
candidate.is_dir() proof unwitnessed (m3, 0 kills).

=== TURN 8 (2026-07-26) — 3 CONFIRMED_DELETE, 1 REFUTED_KEEP ===
Applied cuts (commit 3cc147b, full batteries in the message):
- test_maintainer_preflight.py::test_staleness_disarmed_suppresses_stale
- test_metric_revert.py::test_consecutive_duplicate_statuses_collapse
- test_metric_fix_efficacy.py::test_clean_on_stage_mismatch
New registry carriers (278 entries): the four disarm siblings
(per_schedule_independent M6-sole, suppresses_hung M3/M14-sole,
suppresses_fail M11-sole, disarmed_no_record_silent M12-sole),
test_no_reopen_after_ship_not_a_revert,
test_clean_on_type_mismatch, test_multi_anchor_post_fix_counts_only_tuple_matches
(P4 is now its sole kill — the stage dimension depends on it),
test_bead_sort_order.

REFUTED, hard miss to re-propose:
- test_triage.py::test_colon_format_matches — DISTINGUISHING MUTATION:
  _truncate default width (triage.py `def _truncate(text, width=80)`) mutated
  into the window [62,68] reds it ALONE suite-wide (width 61 reds 3, 79/81/0
  red others or none — the finder's +-1/0 int class could not reach the window).
  Its neighbor test_one_deferred_surfaces_title_and_defer_comment has its own
  sole kill (title-coercion mutation), so the family is mutually protected.

Coverage GAPS logged (deletion-independent; follow-up ticket material):
metric.py _collapse identity + `>` vs `>=` ship-time boundary unpinned
(needs duplicate rows straddling shipped_at); triage open-question fallback
arm never exercised (ordered[-1] choice, _NO_COMMENT replacement,
created_at sort all unpinned); _truncate len<=width short-circuit and
whitespace collapse unpinned; advisor tag keyed on '(advisor)' unpinned;
fix-efficacy STAGE_FALLBACK injected-branch shape unwitnessed.

=== TURN 8 finder near-miss adjudications (verbatim; re-proposing any is a hard miss) ===

--- finder 0 ---
SHAPE (a) — tests pinning test-infrastructure rather than engine behavior: EMPTY, and I believe now closed.

Two AST passes, both negative:
- No test anywhere asserts on `tests/wsfactory.py` output. `grep -rn "render_toml" tests/*.py` matches only wsfactory.py itself (its own definition and internal use). No test reads back a `.flow/workspace.toml` and asserts the factory rendered it; every consumer feeds it to the engine. conftest.py contains exactly one fixture (`_hermetic_git`).
- Transitive-purity pass (script at /tmp/lensAB/infra2.py): for each test file, classify module-level helpers as engine-touching or pure by transitive reference closure over engine imports, then flag tests whose body references NO engine symbol and whose only calls are pure helpers. Exactly three files surfaced, all already adjudicated and none proposable: test_git_hermeticity.py (3 tests over `_config`/`_fresh_repo` — refutation record turn-5 item 8, per-key guards, each probes a different leaked config key); test_plugin_packaging.py (4 tests over `_json`/`_frontmatter_keys` — real-manifest live pins, out of scope by standing rule and among the 9 known scratch failures); test_revise_config.py:116/128 (`_run_apply_floor` subprocess CLI pair — refutation record turn-5 item 9, "process/concurrency singletons"). The pure-helper form of shape (a) is exhausted: every candidate the definition admits was already refused.

SHAPE (b) — duplicated golden/corpus expectations. New detector, then hand adjudication.

Detector (new lens, deliberately fuzzier than sweep 2's GAP-1 exact-structural-identity scan, which reported "10 collision groups, all cross-file" and therefore missed every same-file near-twin): per file, normalize each test body to an AST skeleton with all constants erased, cluster same-file pairs by difflib ratio >= 0.72-0.80, and flag pairs where the candidate's normalized assertion set is a subset of the dominator's. Two variants: strict-subset-of-sets (231 rows) and equal-sets-with-more-assert-statements (the variant that surfaces P1 — a candidate whose textually-different negative assertion normalizes to the dominator's). Both proposals came out of this; a third (test_flow_beads_create acceptance_invariant, below) was independently reached by a raw shared-golden-literal scan and then refuted.

REFUSED, each with the distinguishing mutation, all run in the scratch copy with caches cleared:

1. test_flow_beads_create.py::test_acceptance_invariant_embedded_in_description (asserts `desc == "body text\n\nACCEPTANCE-INVARIANT: all amounts positive"`) vs ::test_acceptance_invariant_collapsed_to_single_line (asserts the same golden template with a multi-line invariant collapsed, so semantically a superset). Battery on flow_beads_create.py:242-245 — N1 `if acceptance_invariant:` -> `if True:` reds absent_omitted alone; N2 drop the `" ".join(...split())` collapse reds collapsed alone; N3 separator `\n\n`->`\n`, N4 prefix rename, N5 order swap each red embedded+collapsed. N6 predicate strengthened to `if acceptance_invariant and "\n" in acceptance_invariant:` reds test_acceptance_invariant_embedded_in_description ALONE (1 failed, 17 passed). SOLE KILL -> KEEP. Applying the same predicate-strengthening standard I used to save P1's four siblings (M11/M12/M14) forbids cutting this one.

2. test_reflect_inputs.py::test_bundle_recurrence_degrades_to_empty_on_no_workspace vs ::..._on_detector_crash — byte-identical assertion triples (`payload["friction_recurrence"] == []`, `payload["ticket"] == "FT-1"`, `"state" in payload`). REFUSED on read of reflect_inputs.py:176-203: one `try:` with a single `except Exception: return []` swallows both arms, so narrowing it to `except (_memory_paths._MemoryConfigError, OSError)` reds the detector-crash test alone and narrowing to `except ValueError` reds the no-workspace test alone. Symmetric two-arm witness of a deliberately unenumerated exception surface (the docstring says so). Same class as the record's already-refuted "short-circuit collisions on one source line" family.

3. test_pr_body.py::test_flatten_details_basic_wrapper_to_heading vs test_create_pr.py::test_bitbucket_authored_body_details_flattened — 5 byte-identical golden assertions (`<details>`/`</details>`/`<summary>` absent, `"### run: 3 passed (1s)" in`, `"```\nline a\nline b\n```" in`); the create_pr test adds a sixth (footer). Battery: R1 flatten becomes passthrough -> 5 kills; R2 `###`->`##` -> 5; R3 wrapper body dropped -> 3; R4 single newline after the heading -> 0; R5 authored-body path skips `flatten_details` (create_pr.py:111) -> reds test_bitbucket_authored_body_details_flattened ALONE; R6 regex loses its `\b[^>]*` tolerance -> 0; R7 greedy body -> 2 (multiple_blocks + enforce_cap). So the unit test is mechanically dominated and would pass all three bar clauses. REFUSED anyway on the refutation record's own note-1 rule: the fence-preservation half of its claim would survive only in the bitbucket integration pair, leaving a pure function hostage to one backend's dispatch path — the same correlated-deletion shape the record already honored. Flagging rather than filing; a human who disagrees with that precedent has the battery ready.

4. test_metric_revert.py::test_reopen_reclose_before_ship_not_counted — the OTHER 3-assert subset of test_no_reopen_after_ship_not_a_revert, also never a sole killer (Q2/Q4/Q5/Q6/Q7 all red it with siblings; its empty post-slice is indistinguishable from the candidate's). NOT filed: it is the co-witness that keeps Q2 and Q7 (the `when > shipped_at` filter) caught after P2 lands, and unlike P2 its named subject IS exercised — Q2 reds it. Cutting both would drop the pair to a single witness of the ship-time filter. Recorded so a later round does not re-walk it as fresh.

5. Detector rows opened and closed on reading, each with the distinguishing arm: test_validate_workspace.py models cluster (:499/:509/:526/:615/:628/:639) — six DIFFERENT error-message goldens ("launches no agent", "no such role", "unknown field", "model string or a role-keyed table", "string hint", "not in [pipeline].stages"), no overlap; test_queue_drain.py:110 deferred-bead — corpus-membership pin on the `deferred` state (dropping it from the inactive set reds it alone), the record's already-refuted "varied value IS the claim" family; test_metric_friction.py:98 window_half_open — sole witness of the `since <= ts < until` boundary, and :153 unparseable_or_missing_ts_skipped — sole witness of the ts-parse skip; test_forge_github.py:178 pr_info_reads_merged_state — sole witness of the ANY-state read (`if state != "OPEN": return None` reds it alone, and its comment says so); test_stage_merge.py review_brief probe cluster (:242/:270/:283/:312/:341) — five distinct freshness statuses; test_cockpit.py — five disjoint claims, the dedup test uniquely asserting `pending_mutations == 2` alongside `pending_targets == ("FT-1",)`.

SCANS NOW EXHAUSTED (do not re-walk): the pure-test-infra-helper population (all three files enumerated above); direct assertions on wsfactory/conftest output (none exist); the shared-golden-literal census over assert statements (all string constants >= 18 chars appearing in 2+ test functions, 60 top groups triaged — the residue is shared corpus keys and per-schedule/per-subcommand mirrors already adjudicated in turn 5); the fuzzy same-file structural-twin detector in both variants (231 + 21 rows), which is the lens that should be considered spent for assertion-subset domination.

NO REPO STATE MODIFIED: every mutation and both deletions ran in the scratch copy at /var/folders/.../tmp.5008tWix9K/flow; `git status --porcelain` in the worktree is empty at d61dc5a. Scratch-copy baseline is the documented 9 real-checkout path pins, filtered from every kill set. Warning for anyone re-running: `cp -R` carries `__pycache__` along and a same-length mutation can be masked by stale bytecode — the run harness clears caches before every invocation.

--- finder 1 ---
LENS (new, not on the exhausted list): MUTANT KILL-VECTOR DOMINATION. Prior rounds used coverage arcs as a proxy for "what a test can detect". This round built the real thing: a mutation kill matrix. Phase 1 generated 26,183 single-site source mutants across 82 engine modules and ran 11,416 of them (coverage-directed: each mutant runs only the tests whose branch arcs touch the mutated line), giving a per-test kill vector. A test with ZERO uniquely-killed mutants is a candidate; a test whose kill set is contained in one other test's is a single-dominator candidate. Phase 2 re-ran, per candidate, an exhaustive line-restricted battery (every mutation class at every line the candidate executes, including the classes phase 1 lacked: charset-character drop, regex-alternative drop, collection/dict element drop, predicate pinned to True/False) plus hand mutations for the classes no generator expresses. Artifacts: /tmp/mutlens/matrix.json (kill matrix), o_g*.jsonl (11,416 mutant records), gen2.py + run2.py (the two-stage sole-kill prober), analyze2.py.

EIGHT CANDIDATES EXAMINED. Two filed above; six refused, each with the distinguishing mutation pasted so a later round can skip them:

1. tests/test_lint_comments.py::test_sentence_final_breaks_pass (0 sole kills over 208 automated mutants; single dominator test_dogfood_engine_and_test_file_clean, whose kill of the '.' arm is real -- dropping '.' from `stripped[-1] in ".!?:;"` makes lint_comments.py's OWN module docstring line 9 under-fill, verified by running lint_file on the mutated engine). REFUSED on two hand sole kills: M3 "exempt only when the NEXT line is not itself sentence-final" reds it alone (it is the only fixture whose two lines BOTH end in '.'), and M9 "exempt only when cur is shorter than the next line" likewise. Note the measured asymmetry for the record: dropping ':' from the same charset reds test_underfill_colon_ended_intro_line_passes ALONE, so that neighbour is independently safe.

2. tests/test_lint_comments.py::test_list_block_passes -- NO sole kill found (238 automated mutants at all 111 lines it executes + 5 hand mutations on the _is_structured/_skip_for_fill path: L1 drop '-' from "-*|>#=:" -> 2 red (dogfood + candidate); L2 marker must be followed by a space -> 1, dogfood only; L3 marker honored only on the continuation line -> 1, dogfood only; L4, L5 -> 0). REFUSED anyway, on grounds the bar leaves to judgment: (a) its kill set is not inside any single test's -- it needs dogfood UNION 4 others, i.e. union domination, an exhausted lens; (b) deleting it leaves the '-' member of the structured-marker charset witnessed only by dogfood's incidental engine prose, while its sibling test_underfill_skips_star_bullets_and_numbered_rows keeps a genuine sole kill (dropping '*' reds it alone). Cutting one charset member's intentional witness and not the other is arbitrary.

3. tests/test_queue_drain.py::test_classify_reap_active_bead_with_worktree (0 sole kills over 110 automated mutants including predicate pinning; kill-set EQUAL to test_classify_reap_launch_key_without_worktree at 79). REFUSED: two hand coupling sole kills -- `"has_worktree": key in worktree_keys and (bead_status.get(key) or "") not in _ACTIVE` reds it alone, and the mirrored coupling of bead_active to worktree_keys reds it alone. It is the suite's only bead with bead_active True AND has_worktree True, hence the sole witness that the two fields are computed independently. Same shape as the record's compose_commit test_includes_ticket refusal. (For reference: hardcoding has_worktree True reds test_classify_reap_launch_key_without_worktree + test_cli_drops_launch_key_with_merged_pr; hardcoding False reds 3.)

4. tests/test_lifecycle.py::test_request_after_scope_approval_is_rejected_for_incomplete_run (0 sole kills over 641 automated mutants; single dominator is the CLI-layer test_reduce_rejects_invalid_request_as_structured_error, which replays the same payload through the CLI -- the cross-layer duplicate shape the brief suggested). REFUSED: its second arm (lease_state="live_foreign") is a precedence witness. H1 "move the `lease_state == live_foreign` gate above the approved-scope rejection" reds it ALONE; H4 "skip the rejection while a foreign lease is live" reds it alone. Dropping the rejection or rewording its message reds both it and the CLI test, so the shared claim is doubly covered, but the ordering claim is not.

5. tests/test_senses_deadman.py::test_dry_run_unavailable_ignores_stale_nightly_completion (kill-set equal to test_dry_run_unavailable_omits_producer_clause_when_nightly_ran at 23; the two fixtures differ only in the nightly timestamps). REFUSED: gating `if "nightly" in liveness.get("schedules", {})` on `age_hours <= 24` reds it ALONE. It is the sole witness that senses applies no freshness threshold to the nightly-presence signal -- which senses_deadman.py's own docstring states is maintainer_preflight's job.

6. tests/test_tracker_cli.py::test_transition_enqueue_exception_swallowed (kill-set equal to test_transition_failure_without_kind_returns_1 at 16; both only read rc). REFUSED: narrowing `except Exception` to `except ValueError` around the pending-mutations enqueue reds it ALONE. Sole witness that ANY enqueue failure is swallowed rather than propagated.

METHOD WARNING FOR THE NEXT ROUND. Six of eight candidates were saved by mutation classes no AST generator produces: statement REORDER, a guard COUPLED to another in-scope value, and EXCEPTION-TYPE narrowing. The complete matrix still lists 302 unexamined single-dominator candidates with kill count >= 12 (largest clusters: test_init.py ~14 nodes, test_recall.py semantic family ~6, test_metric_revert.py ~4, test_flow_worktree.py ~6) -- but given a 6/8 refusal rate on the same signal, that list should be treated as a screen with a high false-positive rate, never as evidence. The three mutation classes above must be hand-run per candidate before anything is filed.

COVERAGE GAPS FOUND (not deletions; candidate material for a follow-up ticket):
- triage.py: nothing in the suite witnesses that the colon defer format actually MATCHES _DEFER_STEM. Tightening the stem to require "(", or to a `startswith(STEM + " (")` form, or skipping the stem scan on single-comment tickets, each kills 0 tests -- the `chosen = ordered[-1]` fallback masks a stem-match regression on every single-comment fixture. The engine comment at triage.py:34-36 says supporting both forms is deliberate.
- lifecycle.py:146: dropping the `evidence.scope_approved` term from the approved-scope guard kills 0 tests. No fixture has run_state healthy + request True + scope_approved False.
- lint_comments.py:194: two plausible mutations of the structured-marker check (marker must be followed by a space; marker honored only on the continuation line) are caught ONLY by the test_dogfood_engine_and_test_file_clean self-lint, i.e. by incidental engine prose rather than by any intentional test.
- queue_drain.classify_reap: `"pr": pr.get("number") or 0` kills 0 tests.

HOUSEKEEPING: running pytest with cwd = the real scripts dir (as the brief requires) makes some tests write into that cwd. Five UNTRACKED artifacts are now sitting in the read-only worktree at plugins/flow/skills/flow/scripts/: .flow/, FT/, demo/, fleet/, .gitignore. `git status --porcelain | grep -v '^??'` is EMPTY -- zero tracked files modified, HEAD still d61dc5a -- but the content-ownership commit gate reads untracked files as drift, so they should be removed before any commit in this worktree. My attempt to delete them was blocked by the permission classifier, so they are still there. Command to clean: chmod -R u+w .flow FT demo fleet && rm -rf .flow FT demo fleet .gitignore (run from plugins/flow/skills/flow/scripts/).

=== TURN 9 (2026-07-26) — 2 CONFIRMED_DELETE, 0 REFUTED ===
Applied cuts (commit 469a12e, full batteries in the message):
- test_version.py::test_bump_for_type_feat_is_minor (point-in-table shadow of 4 compute/stamp witnesses)
- test_metric.py::test_classify_not_attributed_no_state (doubly-implemented fallback, 2 permanent witnesses)
New lens on record: POINT-IN-TABLE DOMINATION (standalone test whose call site +
argument values coincide with a point an enumerated table/composite already drives).
New registry carriers (283): test_compute_explicit_feat_bumps_minor,
test_compute_empty_string_commit_type_is_unset, test_stamp_feat_writes_minor,
test_classify_malformed_stamp_falls_back_not_attributed,
test_classify_not_attributed_corrupt_state.
Coverage gaps logged: version.py exact-only "feat" match unpinned (prefix/contains
variants all survive); bump_minor parse-guard shape variants survive.

=== TURN 9 finder near-miss adjudications (verbatim; re-proposing any is a hard miss) ===

--- finder 0 ---
MY ANGLE (round 9, not on the exhausted list): **POINT-IN-TABLE / DUPLICATED-POINT DOMINATION.** Every prior lens compared whole test functions (branch arcs, normalized assertion text, mutant kill vectors) or pruned rows *within* one parametrize grid. None asked the inverse question: does a hand-written standalone test occupy a point that some *enumerated table* already contains — a parametrize row, a module-level corpus row, or an inner call made verbatim by a richer composite test? The point matters mechanically: a grid row is a separately-collected node, so it satisfies the bar's "NAMED other test"; and when the candidate's call site AND argument values are identical to a point another test already drives, no single-site mutation can separate them, which is exactly the property eight rounds of kill-vector work kept failing to find by other routes. Assertion text is irrelevant to this relation, which is why lens A's (arcs AND assertion-text subsumption) intersection and sweep 2's literal-erasing variant both missed the filed find: the candidate asserts `bump_for_type(...) == "0.28.0"` while its dominator asserts a five-key compute payload.

METHOD: extracted all 96 parametrize grids across the suite with their rows (AST, /tmp/r9lens/grid.py), plus every module-level corpus feeding one (FIELDS, FORMS, BATTERY, _CASES, _MAIN_GATE_CASES, _REFERENCE_DOC_ENTRIES). Then, per grid, listed the non-parametrized tests reaching the same engine call (/tmp/r9lens/pointgrid.py) and hand-checked whether the standalone's input falls in an enumerated equivalence class. Six candidates examined deeply, one filed, five refused below with the distinguishing mutation. Repo read-only throughout; HEAD 3cc147b, `git status --porcelain | grep -v '^??'` empty.

REFUSED (re-walking these is waste):

1. tests/test_lifecycle.py::test_live_foreign_lease_wins_over_broken_run_until_holder_releases and ::test_deferred_question_wins_before_run_recovery. Screened as table points because the reducer decision table (test_lifecycle.py:64, 18 rows) already carries `(run_state="healthy", lease_state="live_foreign") -> RUNNING` and `(ticket_state="deferred", stored_question=True) -> ANSWER`. REFUSED on reading the inputs: neither is a table point. Both feed a CONFLICTING pair the table deliberately never enumerates — `run_state="failed"` together with `lease_state="live_foreign"`, and `ticket_state="deferred" + stored_question` together with `run_state="failed"`. They are precedence witnesses: any reorder that puts the REPAIR gate above the live-foreign gate, or above the stored-question gate, reds one of them alone (the table has no row where two gates compete). Same class as the record's already-refuted test_hitl_gating ordering trio, and precisely the statement-REORDER class the turn-8 method warning says no generator expresses.

2. tests/test_worker_pool.py::test_recovery_plan_defaults_missing_durable_evidence_to_relaunch, screened against the 6-row `test_driver_failure_recovery_uses_durable_run_evidence` grid (which contains ABSENT -> RELAUNCH and SUCCEEDED -> SETTLED). REFUSED: it calls a different function — `driver_recovery_plan` over a key LIST with an evidence dict missing "FT-1" — not `driver_recovery_outcome`. It is the sole witness both of the missing-evidence default (dropping the `.get(key)` fallback reds it alone) and of per-key result ORDER (`[(key, action) ...] == [("FT-1", RELAUNCH), ("FT-2", SETTLED)]`). Not a table point.

3. tests/test_queue_status.py::test_severity_floor_is_major_plus (5-row grid) plus its two plain neighbours :447 test_slugged_ref_resolves_and_flags_major and :468 test_resolved_and_minor_threads_not_flagged. The clearest point-in-table screen in the suite: the grid's major->flagged row is duplicated by :447, critical->flagged by test_forge_error_on_one_key_does_not_drop_others, and minor/nit->not-flagged by :468 which feeds both severities at once. REFUSED in all three directions. (a) The grid keeps the "unknown" severity row, which nothing else feeds — narrowing the floor from `severity in {"major","critical"}` to `severity not in {"minor","nit"}` reds that row alone; cutting the whole grid is therefore off, and row surgery is the exhausted parametrize-grid lens. (b) :468 is the only fixture with a RESOLVED major thread, so dropping the resolved filter reds it alone. (c) :447 is the only test asserting the thread PROJECTION shape (`threads == [{"id","severity","title"}]`) — test_status_surfaces_parked_reviews reads only key and pr_id — so dropping "title" from the projection reds it alone.

4. tests/test_tracker_link.py::test_cross_backend_read_direction_agrees, screened as the union of two enumerated points: the jira half's `Blocks` inward link duplicates an arm of the registry-protected test_get_issue_populates_links_from_issuelinks (tracker_jira.py:505), and the beads half duplicates test_get_returns_full_ticket_shape's `links[0]["kind"] == "blocks"` / `["to_key"] == "bd-9999"`. REFUSED on the beads half: `tracker_beads.py:267 "from_key": key` is asserted NOWHERE else — test_beads_adapter.py:192-194 and :261 read only `kind` and `to_key` (`grep -n '"links"' test_beads_adapter.py test_jira_adapter.py` confirms). Mutating that producer (e.g. `"from_key": dep.get("target")`) reds this test alone. Recorded for the record: its file-mate test_cross_backend_blocks_direction_agrees IS a clean point-in-table shadow — its beads assertion equals grid row test_beads_link_maps_kind[blocks] and its jira assertion is a strict subset of test_jira_link_maps_kind[blocks]'s body dict — but it is registry-protected (survivor-registry line 69), so it is a hard miss and was not adjudicated further.

5. tests/test_tracker_cli.py::test_transition_hard_failure_with_flag_no_enqueue — a ONE-row grid `[("permission_denied", 4)]` whose point sits inside the 5-row registry-protected test_transition_failure_kind_maps_to_exit. REFUSED: the rc claim is the shadowed half; the other assertion `pending_mutations.list_mutations(tmp_path) == []` is the sole witness that a HARD (non-transient) failure does not enqueue even with `--enqueue-on-transient` set. Widening the enqueue to fire on any failure reds it alone (test_transition_success_with_flag_no_enqueue covers only the success arm, and the transient test asserts the opposite).

ALSO SCREENED AND CLOSED WITHOUT REACHING CANDIDATE STATUS (so a later round can skip): test_version.py::test_bump_patch_malformed_raises vs ::test_bump_minor_malformed_raises — identical row lists `["x.y","1.2","1.2.x"]` and identical `pytest.raises(ValueError, match=...)` oracle, but version.py:74 and :83 are two COPY-PASTED guards, so each grid is sole coverage of its own copy (same shape as the record's turn-5 metric-CLI mirror refusal). ::test_bump_minor_increments_minor_resets_patch — two-point boundary probe (minor=27 and minor=0); a mutation wrong only for minor==0 (`minor + 1 if minor else 2`) reds it alone, and minor==0 is a real input class, not a hardcoded test literal. test_beads_adapter state-normalization and list-transitions tables, test_jira_adapter normalize_state (registry), test_public_commands target-classification and unknown-token grids, test_scrub_ci_skip FORMS, test_model_resolve disabled-value lists, test_observe_ship_event FIELDS: all either registry-protected by name or already adjudicated in the record as corpus-membership pins where the varied value IS the claim.

LENS VERDICT: point-in-table domination is largely spent after this pass. The relation is real and productive — it found the one thing eight rounds of arc, assertion-text and kill-vector screens missed — but the suite contains few tables, and the surviving standalone neighbours are overwhelmingly complementary arms (the negative row of a positive grid) or conflicting-input precedence witnesses that a table by construction cannot hold. The generalizable rule the round establishes, worth carrying forward: **a test is deletable only when another test drives the same call site with argument values in the same equivalence class**; the fastest way to prove or refute that is a degenerate mutation hardcoded to the candidate's own literals — if it still reds a named neighbour, the candidate is a shadow, and if it reds the candidate alone, the input is unique and the test stays.

--- finder 1 ---
MY ANGLE (stated explicitly, and it is not on the exhausted list): GUARD-ATTRIBUTION FIDELITY — find tests that are green for a reason OTHER than the one their name/docstring claims, because their fixture trips a different engine guard than the one they target, or because their nominal claim is never observed by any assertion. This is a semantics-of-the-oracle lens, not a coverage or kill-matrix lens: the screen reads the guard order in the engine and the diagnostic granularity of the oracle, and it is verified by perturbing the TEST'S OWN FIXTURE rather than the engine. Two new sub-instruments came out of it, both fully run and now spent: (i) an INERT-SEED scan (fixture keys the engine never reads anywhere, so no predicate-strengthening mutation on them counts as existing behavior) and (ii) a SEMANTIC ASSERTION-IMPLICATION scan, which closes the one real gap in lens A's method — lens A required AST-normalized assertion-text subsumption, so it structurally could not see `len(x) == 2` implied by `x == [a, b]`, `"foo" in out` implied by `out == "...foo..."`, `x is not None` implied by `x == 5`, or `k in d` implied by `d[k] == v`.

FIRST, A FINDING THAT NARROWS EVERY FUTURE ROUND — 13 TEST FILES ARE PROTECTED WHOLESALE, not individual tests. 25 of the 278 survivor-registry entries do not resolve to any `def test_*(` in the suite. Eleven are visibly truncated function names (e.g. `test_driver_recovery_does_not_accept_disposable_`, `test_reserved_namespaces_cover_every_flow_owned_`). The other 13 are exact test-FILE basenames with no function of that name anywhere: test_dispatch_stage, test_flow_friction, test_flow_worktree, test_forge_cli, test_init, test_maintainer, test_queue_drain, test_recall, test_recall_usage, test_revise_config, test_runtime_layout, test_seam_check, test_worker_pool. I treated all 13 as file-level protections and excluded them (plus the three hard-constraint files and the guard clusters) from every scan. This cost me my most promising early candidate (test_recall.py) and is worth confirming or correcting before round 10 — the honest read either way is that the reachable population is much smaller than 3153.

CANDIDATES OPENED AND REFUSED, each with the distinguishing mutation:

1. The `*_when_no_workspace` config-default family — friction_escalate::test_escalation_k_default_when_no_workspace / test_exempt_anchors_default_when_no_workspace, triage::test_advisor_adjudicates_default_on_when_no_workspace, ::test_adjudicate_hot_default_off_when_no_workspace, create_pr::test_draft_config_default_true_when_no_workspace, ::test_base_config_none_when_no_workspace. Hypothesis: the file-absent, section-absent and key-absent arms converge on one default, so the no_workspace twin duplicates the section_absent twin. REFUTED on reading the readers: every one is `try: config = load_workspace_toml(root) except WorkspaceConfigError: return DEFAULT` written out PER FUNCTION (friction_escalate.py:58 and :79, triage.py:88 and :114 are four separate try blocks, not one shared helper). Removing a single `except WorkspaceConfigError` handler reds that function's no_workspace test and nothing else — a sole kill each. The whole family stays. This closes the family as a class, not just these six.

2. test_recall.py::test_semantic_disabled_flag_off_no_embed_call vs ::test_malformed_config_threshold_semantic_off_still_ranks — the sharpest claim-observation mismatch I found: the first test's name and docstring promise 'never touches the embedder' and its body contains NO embedder observer at all (no monkeypatch, no call recorder); its only asserts are `rc == 0` and `payload[0]["id"] == "a"*16`, byte-identical to its neighbor's, whose fixture is a strict superset (adds `threshold = "high"`). NOT PROPOSED: test_recall.py is one of the 13 file-level registry protections. Recorded because if that read is wrong, this pair is the strongest structural near-twin left in the suite — and either way the 'no embed call' claim is unwitnessed (a gap). For a future round: recall.py:615-623 parses `threshold` EAGERLY, before the `if semantic_on:` branch, so the malformed-threshold test is not input-blind — it exercises the `except (TypeError, ValueError)` arm; the domination would be a union (that arm from the semantic-ON twin, the off-path from the flag-off twin), and union domination is exhausted.

3. test_recall_usage.py::test_record_usage_no_recall_log_writes_nothing — suspected of being green for the wrong reason because it also passes `used_ids=[]`, which looked like a second sufficient cause for the empty result. REFUTED on reading recall_usage.py:201-222: the record list is built from `_surfaced_ids(...)`, and `used_ids` only sets each record's `used` boolean, so it cannot suppress a write. The test is the sole witness that a missing recall log yields no surfaced ids. (File is registry-protected anyway.)

4. test_lint_comments.py::test_discovery_reads_ruff_toml_black_and_editorconfig — flagged by the inert-seed scan because `max_line_length` appears in no engine string literal. FALSE POSITIVE: lint_comments.py:410 reads it inside a raw regex `r"^\s*max_line_length\s*=\s*(\d+)"`, which the literal scan misses. Its three arms (ruff.toml / [tool.black] / .editorconfig) are three separate discovery sources and each is its own sole witness.

5. The `metric.classify_attribution` not-attributed family other than the proposal — test_classify_not_attributed_run_id_mismatch, ::_reflect_not_completed, ::_no_reflect_stage, ::_corrupt_state. All REFUSED: each owns a distinct `return` inside the six-guard ladder at metric.py:192-216, and I measured the sole kills — M4 (narrowing `except (OSError, json.JSONDecodeError)` to `except OSError`) reds test_classify_not_attributed_corrupt_state ALONE, and the record already refused the reflect_not_completed/no_reflect_stage pair on the line-214 isinstance argument. Only the no-state case has no private return to defend it, because metric.py:198-199 is subsumed by the OSError arm below it.

6. test_state.py::test_init_auto_generates_run_id (surfaced by the implication scan as `len(ts.run_id) == 16` implied by `ts.run_id == "0123456789abcdef"`). REFUTED: the dominator SUPPLIES `run_id=` while the candidate omits it, so the candidate is the only test taking the auto-generation arm — and the only test calling `state.init` positionally, which pins the signature order. Dropping the generator reds it alone.

7. test_queue_select.py::test_select_mixed_sessions_only_day_job_consumes_budget (implication scan, coarse `len(out["launch"]) == 1` oracle). NOT PROPOSED: it is a genuine two-term discriminator (concurrency 2 minus one day-job session, evolve lease not counted — both terms are load-bearing, either omission changes the count), and it sits inside the queue/evolve mirrored BUDGET cluster the record adjudicated and refused by name in turn 5.

8. The two snapshot.py rows from the implication scan — test_snapshot.py::test_workspace_edit_drift_names_workspace_toml (implied by ::test_classify_drift_names_components) and ::test_verify_with_no_snapshot_is_absent (implied by ::test_classify_drift_no_snapshot_is_true). NOT OPENED: drift classification is the canonical-snapshot TOCTOU guard, off-limits by the standing rule. Flagged for a human who wants to re-scope that rule, since these are the only two implication-relation rows left in the suite.

SCANS NOW EXHAUSTED (do not re-walk): the coarse-oracle negative-test census (201 nodes: every test whose name signals a refusal/absence and whose asserts contain no string literal, i.e. cannot say WHICH diagnostic fired — enumerated and triaged, the residue is per-guard witnesses); the inert-fixture-seed scan (13 hits, all either deliberate garbage values that ARE the claim — `bogus_stage`, `bogus_extra_key`, `surprise`, `rare` — or regex-read false positives); the semantic assertion-implication scan in all four implication forms across all unprotected files (exactly 6 rows, all listed above); the `no_workspace` / `section_absent` / `key_absent` config-default family as a class; and the shared-asserting-helper delegation scan (tests whose asserts live entirely in a module-level test helper that 2+ tests call: ZERO hits suite-wide).

COVERAGE GAPS FOUND (deletion-independent, follow-up ticket material): metric.py:198-199 `if not state_path.is_file(): return ATTR_NOT_ATTRIBUTED` is DEAD — deleting it outright, or weakening `is_file()` to `exists()`, kills nothing, because the `except (OSError, json.JSONDecodeError)` arm two lines down already returns the same verdict for a missing file. metric.py:192-193's non-str/empty-ticket guard is unwitnessed: flipping its return to ATTR_VIA_FLOW kills nothing (no fixture anywhere feeds a ship event with a missing or non-string ticket). And recall.py's 'semantic off means the embedder is never shelled' contract has no observer in the suite.

NO REPO STATE MODIFIED: every mutation and the deletion ran in the scratch copy at /var/folders/xh/cmw393yj6tqfjyp4fq74bmww0000gq/T/tmp.97X2tGGxkf/flow (left in place, pristine, for verification). In the worktree, `git status --porcelain` is empty for both tracked and untracked paths and HEAD is still 3cc147b. Two harness warnings for the next round: (a) `cp -R` carries `__pycache__`, and a same-byte-length mutation is masked by stale bytecode — my apply script now asserts a byte delta and the runner clears caches every invocation; (b) mise shims REFUSE to execute inside the scratch dir ('Config files ... are not trusted'), which silently no-op'd my first battery and reported a clean 168-passed for all seven mutations — use /usr/bin/python3 for anything that runs with cwd inside the scratch copy, and always verify the mutation landed before believing a zero kill count.

=== TURN 10 (2026-07-26, FINAL) — 1 CONFIRMED_DELETE, 1 REFUTED_KEEP ===
Applied cut (commit 158a34f): test_validate_workspace.py::test_code_review_block_absent_is_valid
(value-trace shadow of registry-protected test_valid_beads_workspace_passes; verifier
independently re-derived trace identity and closed the global-state-order family too).
New lens on record: ENGINE VALUE-TRACE IDENTITY (per-test boundary-value tracing, hashed).

REFUTED, hard miss to re-propose:
- test_metric.py::test_percentile_median_of_odd_list — DISTINGUISHING MUTATIONS: five
  independent sole kills; the candidate is the suite's only percentile observation with
  an integral rank landing on an interior index >= 2 (n=5 pct=50 rank=2.0). Registered.
New registry carriers (285): test_code_review_block_hard_fails_naming_replacement,
test_percentile_median_of_odd_list.

Known blind spots of the trace lens, stated by its own finder (future rounds must NOT
treat trace collisions there as evidence): child-process work (test_flow_launcher shims,
concurrency singletons), values never crossing a traced boundary, reprs > 900 chars.

=== TURN 10 finder near-miss adjudications (verbatim; re-proposing any is a hard miss) ===

--- finder 0 ---
MY ANGLE, STATED EXPLICITLY. The brief asked for a targeted completeness audit of the 5 highest-count remaining files, hunting one suspicion per file. I built a same-file pair screen deliberately complementary to the exhausted assertion-subset detector (/tmp/r10/screen.py): it scores pairs on normalized SETUP identity (the fixture half, which turn-8's assertion-subset variants did not key on), on whole-body skeleton similarity, and on raw ASSERTION identity, then ranks. That is the screen. The adjudication instrument that actually decided everything was the turn-9 rule applied mechanically: same call site + argument values in the same engine equivalence class, proven or refuted by a degenerate mutation hardcoded to the candidate's own claim.

FINDING THAT REDEFINES THE ROUND -- THE LITERAL TARGET SET IS EMPTY. Turn-9 finder 1 asked for its file-level-protection read to be confirmed or corrected before round 10. I confirmed it, in both directions:
  $ for n in test_dispatch_stage test_flow_friction test_flow_worktree test_forge_cli test_init test_maintainer test_queue_drain test_recall test_recall_usage test_revise_config test_runtime_layout test_seam_check test_worker_pool; do grep -rn "def ${n}(" tests/*.py; done
    -> NO def, all 13
  $ same 13 as "$n.py"  -> FILE EXISTS, all 13
Corroboration in the other direction: the registry refers to functions inside a NON-protected file using node-id form (lines 261-283: tests/test_evolve_reap.py::..., tests/test_metric.py::...). A bare basename therefore means the file. Counting `def test_` per file against that:
    131 test_flow_worktree      FILE-PROTECTED
    108 test_seam_check         FILE-PROTECTED
     92 test_lease              HARD-CONSTRAINT
     87 test_dispatch_stage     FILE-PROTECTED
     79 test_init               FILE-PROTECTED
     78 test_recall             FILE-PROTECTED
So all five files the brief names are off-limits, and the reachable population is ~2,700 nodes across 98 files, not 3,151. I audited the five anyway (below, no proposal possible) and moved the empirical work to the highest-count reachable files, which is where the proposal came from.

AUDIT OF THE FIVE LITERAL TOP FILES (one suspicion each, adjudicated by reading the engine; no battery, because a proposal is barred either way):
1. test_flow_worktree.py (131) -- strongest pair: :150 test_bootstrap_refuses_non_feat_branch vs :159 test_bootstrap_refuses_other_tickets_feat_branch (byte-identical fixture, assertion ratio 0.98). KEEP: flow_worktree.py:1198 is one compound predicate, `if not branch.startswith(f"feat/{ticket}")`. Weakening it to `startswith("feat/")` reds the FT-2 test alone; weakening it to `ticket in branch` reds the fix/ test alone (fix/FT-1-thing contains FT-1). Symmetric two-arm witness of a compound guard.
2. test_seam_check.py (108) -- strongest pair, and the only one in the five I could not talk myself out of: :991 test_exact_two_distinct_citations_clean vs :1003 test_non_registry_token_does_not_inflate. Raw assertion sets are IDENTICAL (`over == {}`), normalized setups 0.99, both sit at exactly the count-2 boundary; the only arm :1003 owns alone is the registry intersection (drop it and its 3rd foreign token pushes the count over, redding :1003 alone), while I could construct no mutation redding :991 alone. NOT PROPOSED and NOT battery-tested: the file is protected wholesale. Recorded so a human who re-scopes that protection has the pointer -- this is the strongest surviving structural near-twin I found in the protected set.
3. test_dispatch_stage.py (87) -- the top screen rows are the canonical-snapshot TOCTOU cluster (:998/:1237/:1519, identical assertions), off-limits by standing rule. Next: :416 test_next_routes_skill_handler vs :432 ..._without_args. KEEP, symmetric: `"skill_args": args or ""` reds the without_args test alone (it asserts `is None`), a hardcoded None reds the with_args test alone.
4. test_init.py (79) -- top pair :1309 test_jira_personal_checkpoint_mode_rejected vs :1314 test_beads_work_checkpoint_mode_rejected. Already adjudicated and refused in the record (lens A, "init _resolve_checkpoint_mode jira/personal vs beads/work" -- corpus rows where the varied value IS the claim). No fresh work done.
5. test_recall.py (78) -- top cluster is the 5-test tokenize family (:50/:54/:58/:64/:68), pairwise setup-identical. KEEP: each pins a disjoint arm of one regex+normalizer -- `.lower()` (basic), punctuation split (collapses), NFKC (full-width fixture), the empty-input no-crash path, and `_` inside the token charset (dropping `_` reds preserves_word_underscores alone).

REACHABLE-FILE AUDIT (where the empirical work went). Largest reachable files, with lens B's ten-largest walk subtracted, put test_jira_adapter.py (50) as the biggest file never walked end-to-end, followed by test_memory_append.py (46), test_validate_workspace.py (45), test_metric.py (44), test_tracker_cli.py (44). Two suspicions opened, both REFUSED with a measured sole kill; one file yielded the proposal.

A. test_jira_adapter.py -- :269 test_retry_after_seconds_garbage_falls_back_to_default vs :273 ..._none_falls_back_to_default (byte-identical one-line bodies, same `== 1.0` oracle). Both KEEP, each with a measured sole kill:
   J2 `except (TypeError, ValueError)` -> `except ValueError` at tracker_jira.py:135 ... n=0 (nothing notices)
   J2 `except (TypeError, ValueError)` -> `except TypeError`  ... n=1 :: test_retry_after_seconds_garbage_falls_back_to_default   [SOLE]
   J4 `if value is None: return default` -> `... return 0.0`  ... n=1 :: test_retry_after_seconds_none_falls_back_to_default    [SOLE]
   (J1, deleting the None guard outright, reds the None test plus test_transition_persistent_429_raises_tracker_error -- so the None arm is co-witnessed, but J4 proves it still owns a private kill.) Gap logged: J5 widening the guard to `if not value` kills nothing -- no fixture feeds an empty-string Retry-After.

B. test_memory_append.py -- :51 test_compute_id_distinct_for_different_types vs :57 ..._for_different_namespaces (setup 0.94, body and assertion sets IDENTICAL, both just `a != b`). REFUSED, and the measurement inverts the suspicion:
   M1 drop `namespace` from memory_append.py:108 ... n=3 :: distinct_for_different_namespaces + test_ansi_stripped_from_storage_but_id_stays_raw_based + test_clean_body_round_trips_unchanged_with_stable_id
   M2 drop `type_`                              ... n=3 :: distinct_for_different_types   + the same two golden-id tests
   M3 drop `ticket`                             ... n=2 :: ONLY the two golden-id tests
   M4/M5 reorder the concatenation              ... n=2 :: ONLY the two golden-id tests
Neither candidate has a sole kill, but the only other killers are two tests that hardcode a 16-hex sha -- undifferentiated change detectors that fire identically for a dropped field, a reordered field, or a changed normalizer. The two distinct_for_* tests are the suite's only field-level ATTRIBUTION witnesses, so deleting either destroys the ability to say which field left the hash, and deleting one but not the other is arbitrary. M3 proves the family is under-covered, not over-covered: there is no test_compute_id_distinct_for_different_tickets, so `ticket` participating in the id is unwitnessed except by the change detectors.

C. test_validate_workspace.py -- top screen pair :565 test_label_facets_non_list_fails vs :581 ..._non_str_element_fails (identical assertion sets). Not re-walked: the record already refuses exactly this pair in lens A ("dropping isinstance(list) lets the scalar 'form' pass as an iterable of str").

COVERAGE GAPS FOUND (deletion-independent, follow-up ticket material -- all measured, all n=0):
- metric.percentile never sees an unsorted list whose VALUE is checked: `ordered = sorted(values)` -> `list(values)` kills nothing (P05). test_percentile_does_not_mutate_caller_list feeds [30,10,20] but asserts only non-mutation. A 3-element unsorted fixture with a value assertion closes it.
- metric.percentile `hi = math.ceil(rank)` -> `hi = n - 1` kills nothing (P26): every fixture either has hi == n-1 already or an interpolation weight of 0. A 4-element list at p50 closes it.
- metric.percentile n==1 arm is untestable as written: `if n == 1` -> `if False` (P12) and `return float(ordered[0])` -> `ordered[-1]` (P14) both kill nothing, because the general path returns the same value for n==1. The guard is a shortcut, not a behaviour.
- tracker_jira._retry_after_seconds: widening the None guard to `if not value` kills nothing (empty-string header unwitnessed).
- memory_append.compute_id: `ticket` participating in the id is witnessed only by sha-literal change detectors.

SIDE FINDING THE PARENT SHOULD ACT ON -- 114 UNOWNED FILES ARE COMMITTED AT HEAD. Turn 8's finder flagged five pytest artifacts it could not delete (the permission classifier blocked its rm). The next applied commit swept them in:
  $ git log --oneline -3 -- plugins/flow/skills/flow/scripts/demo .../FT .../fleet
    3cc147b tests: cut three dominated tests, keep one refuted candidate (turn 8, 3 nodes)
  $ git ls-files .../demo .../FT .../fleet .../scripts/.gitignore | wc -l
    114
These are tracked, not untracked, so `git status` is clean and the content-ownership gate will not flag them again -- but scripts/demo/friction.jsonl, scripts/FT/, scripts/fleet/ and a generated scripts/.gitignore are now repo content on this branch. They should be removed before the branch is proposed for merge. (Reminder from the memory index: chmod -R u+w first, some CAS blobs are 0444.)

SCANS NOW EXHAUSTED (do not re-walk): the same-file FIXTURE-identity screen in all three variants (setup-identity, whole-body skeleton, raw-assertion identity) across all 114 test files -- this is the complement of turn-8's assertion-subset detector and it is spent; the metric.percentile equivalence-class partition (56 + 10 mutations, complete over the function's 8 statements in every class including reorder and coupled-guard); tracker_jira._retry_after_seconds; memory_append.compute_id. And the registry's file-level protection question is now CLOSED as confirmed, which is the single most useful thing a round-11 (if any) should inherit: the top five files by test count are unreachable, so any further round must start from test_diff_extract (68, and it is the content-ownership guard) or below.

--- finder 1 ---
MY ANGLE (round 10, explicitly not on the exhausted list): ENGINE VALUE-TRACE IDENTITY. Every prior lens compared control flow (branch arcs), aggregated detectability (mutant kill vectors), source text (structural twins, assertion-text/implication subsumption), or table membership. None recorded the VALUES crossing the engine boundary. I built a pytest plugin (/tmp/r10lens/traceplug.py) that, per test, records with sys.settrace: every engine function call with its argument reprs, every engine return value, every exception (type + message) raised inside engine frames, and every outbound call the engine makes into a test-side stub with that stub's args AND return. Noise (tmp paths, 0x ids, 16/40-hex run ids, ISO timestamps, epochs) is normalized, then the event sequence is hashed. Two tests with the same digest drove literally the same engine computation with the same values, so the deletion question collapses to oracle strength alone — the one relation that is decisive rather than suggestive.

METHOD AND HONEST INSTRUMENT VALIDATION. Full traced runs: 3151 nodes, 3047 with non-empty engine traces, baseline preserved (9 known path pins) on every run. I found and fixed three blind spots by checking pairs that MUST differ: (1) exceptions were invisible, so every raising pair collided (test_workspace load_missing vs load_parse_error, test_registry, test_memory_append) — added exception events; (2) stub RETURN values were invisible, only their args — added; (3) the big one: `co_argcount` excludes keyword-only args, so flow's pervasive `*, run_id=None` style made e.g. test_fleet::test_deregister_run_roundtrip and ::test_deregister_run_runid_gated byte-identical while one deletes the entry and the other must not. Fixed by including co_kwonlyargcount plus *args/**kwargs. Each fix split real groups: colliding groups with >=1 unprotected member fell 64 -> 56 -> 41, and the setup-differing pair count 112 -> 100 -> 76. REMAINING KNOWN BLIND SPOTS, stated rather than papered over: work inside child processes (all test_flow_launcher shim collisions and the concurrency singletons are therefore false and I did not adjudicate them), values that never cross a traced boundary (engine locals flowing straight into C-level stdlib I/O — this is exactly why the metric ttp pair collided), and repr truncation at 900 chars (it faked the test_memory_append cap pair [5119] vs [5120]).

Then the decisive sub-relation, run as a scan (/tmp/r10lens/subset.py): within trace-identical groups, candidates whose assertion set is a STRICT SUBSET of a trace-mate's, with `assert x, msg` normalized to drop the message node — the normalization gap that hid the filed find from lens A. Exactly TWO hits suite-wide. One is the proposal; the other is refuted below. I also ran the complementary scan (trace-identical with DIFFERING setup: 76 pairs, i.e. the author varied a seed the engine never saw) and walked it by family.

FIVE CANDIDATES EXAMINED DEEPLY. One filed; four refused with the distinguishing evidence pasted:

1. tests/test_metric.py::test_ttp_non_dict_plan_stage_skips_not_crashes — the OTHER strict-subset hit (2 of 5 asserts, dominator ::test_ttp_skips_missing_timestamp, identical 18-event trace, digest f7d48dff94ff381077f2). REFUSED, SOLE KILL MEASURED: metric.py:336 `plan_stage = stages.get("plan") if isinstance(stages.get("plan"), dict) else {}` -> `stages.get("plan", {})` reds it ALONE (10 failed / 3141 passed = the 9 pins + this one, /tmp/r10lens/kill_MTTP2.txt). Note the equivalent-mutant trap: the softer `stages.get("plan") or {}` kills NOTHING, so a generator that only emits the `or` form would have scored this test as dead. Its trace collides only because the differing seed (plan=None vs plan without started_at_iso) is consumed inside one function by a C-level dict lookup — the documented blind spot, not a redundancy.

2. tests/test_metric_arm_compare.py null-measure family — ::test_time_to_pr_null_when_none_measurable, ::test_interventions_null_when_none_carry, ::test_completion_rate_null_when_none_carry, ::test_reverts_undecidable_skipped, all one trace (72 events, digest 5dba40c98f1adac9e71f). The trace identity is a real finding about the fixtures: `_patch_history(monkeypatch, {})` and `_patch_history(monkeypatch, {"FT-1": None})` are the SAME input to the engine (the lookup returns None either way), so the four are one scenario. REFUSED: each asserts a different key of the same payload, and no assertion-subset relation exists (the subset scan returned none here). Measured for the record: metric.py:1390 `"completion_rate": (merged / len(outs) if outs else None)` -> `else 0.0` reds test_completion_rate_null_when_none_carry plus test_verdict_axis_time_to_pr_favors_flow (2 kills) — the surviving co-witness sits in a DIFFERENT trace group, so cutting on that basis would be the exhausted pairwise-overlap lens, not mine.

3. tests/test_triage.py::test_list_default_makes_no_ready_call vs the registry-protected ::test_no_deferred_prints_sentinel — setup byte-identical (setup-sim 1.00), trace identical (42 events, digest b38e6013edf1e166e772). REFUSED: its oracle reads the stub call log (`not any("ready" in c[0] for c in runner.calls)`), which no trace-mate reads, so there is no assertion domination. Measured: triage.py:385 `include_ready=args.ready` -> `include_ready=True` reds 12 tests including the protected sentinel test (/tmp/r10lens/kill_MTRIAGE.txt) — but only because the _FakeRunner's fixed 3-response queue is exhausted by the extra `bd ready` call. That is incidental infrastructure coupling, the correlated-deletion/hostage shape the record already honors (note 1): give the fake a default response and the 'default mode issues no ready call' invariant would go unwitnessed.

4. tests/test_beads_adapter.py capabilities trio (::test_capabilities_advertise_14_closed_enum_entries, ::test_only_comments_markdown_and_resolutions_supported, ::test_beads_adapter_is_structural_tracker; 8 events, digest 64c67e1ef38ece2de575) and its jira mirror (::test_capabilities_cover_closed_enum, ::test_jira_adapter_is_structural_tracker). REFUSED on reading: three disjoint claims over one capabilities call — the closed NAME set (len 14 + exact set), the per-entry SUPPORTED booleans for 6 entries, and Protocol conformance (`isinstance(adapter, t.Tracker)`, the only suite-wide witness that every protocol method exists). No assertion-subset relation. The jira/beads mirroring is the record's already-refused separate-implementations family.

5. tests/test_create_pr.py open-PR family (::test_creates_when_no_existing_pr, ::test_push_uses_explicit_refspec, ::test_built_scrubbed_body_reaches_open_pr, ::test_reviewers_set_on_open, + protected ::test_open_draft_by_default; 58 events, digest e9d3d6cf3642a878c6b1) and the existing-PR pair (::test_idempotent_reuses_existing_pr, ::test_reviewers_not_set_on_existing_pr). REFUSED: one scenario, but each test asserts a distinct observable of it (returned URL, `len(fg.opened) == 1`, exact push argv, scrubbed body, `reviewers_set == ["1"]`, `reviewers_set == []`). test_creates_when_no_existing_pr is only union-dominated (its push claim by the refspec test, nothing else asserts opened-exactly-once) and union domination is exhausted; on the existing-PR pair, hoisting set_reviewers above the early return reds test_reviewers_not_set_on_existing_pr alone while returning a fresh URL reds test_idempotent_reuses_existing_pr alone.

SCANS NOW EXHAUSTED (do not re-walk): the value-trace digest scan itself, in all four instrument generations (the artifacts are /tmp/r10lens/t5.json for the final one, plus traces.json/traces2/t4 for the progression); trace-identical + strict-assertion-subset with message nodes stripped (exactly 2 hits suite-wide, both adjudicated above); trace-identical + differing-setup (76 pairs, walked by family — the residue is stage_merge probe/execute/cover clusters and metric not_attributed, all already adjudicated in the record, plus the subprocess-mediated flow_launcher and concurrency groups my instrument cannot see). The generalizable rule this round adds: trace identity is the only screen that mechanically discharges the sole-kill clause — when two tests drive identical engine values and the candidate's assertion set is a subset, no mutation of existing behavior can red the candidate alone, because its oracle is a projection of the dominator's on the same values.

COVERAGE GAPS FOUND (deletion-independent, follow-up ticket material): neutralizing validate_workspace._validate_code_review_block entirely is caught by exactly ONE test (test_code_review_block_hard_fails_naming_replacement) — the removal-hard-fail invariant rests on a single node. metric.py's plan/create_pr `isinstance(..., dict)` guards admit an equivalent mutant (`or {}` kills nothing), so the guard's shape, as opposed to its presence, is unpinned. triage's 'default mode makes no bd ready call' invariant is observable only through _FakeRunner queue exhaustion, so it would silently stop being tested if the fake gained a default response.

NO REPO STATE MODIFIED. Every mutation, the deletion and the restore ran in the scratch copy at /var/folders/xh/cmw393yj6tqfjyp4fq74bmww0000gq/T/tmp.bl3vzesQJL/flow (left pristine and re-verified at baseline: 9 failed, 3142 passed). In the worktree, `git status --porcelain` is empty for tracked AND untracked paths and HEAD is still 469a12e. Harness notes for anyone re-running: mise refuses to execute with cwd inside the scratch copy ('Config files ... are not trusted'), so run from the real scripts dir with absolute scratch test paths; `--deselect` with an absolute scratch nodeid silently no-ops on macOS because pytest resolves /var -> /private/var, so post-deletion checks must delete the function (my /tmp/r10lens/delfn.py) rather than deselect it; and tracing adds ~25% runtime, which flaked 5 timing-sensitive tests on one run — the reported traces come from runs whose failure set is exactly the known 9.
